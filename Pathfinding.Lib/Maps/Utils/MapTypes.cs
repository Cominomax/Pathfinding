namespace Pathfinding.Lib.Maps.Utils
{
    /// <summary>
    /// Cannot infer map from file so got to specify it. 
    /// </summary>
    public enum MapTypes
    {
        Grid,
        Graph,
    }
}